#include "main.h"
/**
 * mul is a function that multiplies  two integers
 *@a: first number
 *@b: second number
 * Return: returns result
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
